import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.jadeAmber.com',
  appName: 'jade amber',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
